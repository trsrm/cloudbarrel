# Core

This directory will hold portable CloudBarrel behavior after the plain Linux migration proves its interface.

Do not add placeholder modules. The first implementation must cover a complete Job and Run path through Preflight, versioned transfer, durable result, and failure reporting while continuing to use `rclone` as the transfer engine.
