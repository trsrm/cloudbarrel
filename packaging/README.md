# Packaging

This directory will hold installation and release packaging when an executable CloudBarrel slice exists.

The expected order is native Debian-compatible packaging, generic container packaging, then platform metadata. Do not add empty package definitions before there is a runnable artifact.
